# mate-sdk

Public Python SDK for Mate module authors. Re-exports `Module`, `ModuleContext`, the `@route` / `@on_event` / `@job` decorators, and the typed event/capability registry.

The SDK surface is built out in phase 5 of the platform implementation; this package currently exposes only the namespace placeholder so the workspace resolves.
